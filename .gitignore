# dependencies
/node_modules
/.pnp

# next.js build output
/.next/
/out/
/build

# secrets — NEVER commit these (set them in Vercel instead)
.env
.env*.local
.vercel

# logs and os junk
npm-debug.log*
.DS_Store
*.pem